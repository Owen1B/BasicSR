# Legacy Scripts

This folder contains archived scripts for historical experiment pipelines.

- They are not part of the active converged mainline.
- Paths/options referenced inside may point to `options/_legacy/*`.
- Keep for reproduction/reference only.

Archived families include:
- `spect_selfsup` experiment runners/checks
- NEMA-specific reconstruction/monitor scripts
